@model IEnumerable<ProsumerGrid.Models.Trade.TradeDTO>

@{
    ViewBag.Title = "Overview";
}

<h2>Overview</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Prosumed)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MonetaryValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TradeTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProsumerId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Prosumed)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MonetaryValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TradeTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProsumerId)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
        </td>
    </tr>
}

</table>
